import React from 'react';
import Button from '@/components/common/button';

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <Button>버튼</Button>
    </div>
  );
};

export default page;
